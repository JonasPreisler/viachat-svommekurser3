<%= image_tag @speaker_image.image %>
<%= simple_form_for(@speaker_image) do |f| %>
  <div class="field">
    <%= f.label :image %><br>
    <%= f.file_field :image %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>