<div class="desktopHide">
  <div class="row">
    <div class="col-md-6 col-sm-6">
      <h1>Arrangementer</h1>
    </div>
    <div class="col-md-6 col-sm-6">
      <%= link_to 'Legg til arrangement', new_event_path, class: 'btn-primary2 shadow pull-right' %>
    </div>
  </div>
</div>

<div class="mobileHide">
  <center>
    <h1>Arrangementer</h1><br />      
    <%= link_to 'Legg til arrangement', new_event_path, class: 'btn-primary2 shadow' %>
  </center><br />
</div>

<% unless current_user.events.any? %>
  <div style="font-size:20px;text-align: center;padding-top:30px">Hei! 🙂 Legg til ditt første arrangement.
  </div>
<% else %>
  <br />

  <% @events.order('created_at DESC').each do |event| %>
    <div class="row" style="background: #fff;border-radius:10px;color:#1c1c1c;border: 1px solid #c3c2c2">


      <div class="desktopHideEvents">
        <div class="row">
          <div class="col-xs-6" style="padding: none">
            <div onclick="toggle_div('programs-<%= event.id %>');">
              <%= image_tag event.image.url(:messenger), class: 'event-image' %>
            </div>
          </div>
          <div class="col-xs-6">
            <div class="col-xs-6">
              <div style="font-size:35px;font-weight:700"><%= event.name %></div>
              <div style="color:#c3c2c2;font-size: 12px"> ID: <%= event.id %></div>
              <div style="font-size: 12px">
                <strong>Adresse:</strong> <%= event.address %><br />
                <strong>Billettkjøp:</strong> <%= event.ticket if event.ticket %><br />
                <strong><%= link_to "Kort oversigt", event if event.map %></strong><br />
                <%= event.programs.count %> program<%= 'mer' if event.programs.count > 1 %>
              </div>

              <% if event.programs.any? %>
                <div onclick="toggle_div('programs-<%= event.id %>');">
                  <div class="btn-green-days box" style="cursor:pointer;background:#c3c2c2;width:60%;margin:4px">
                    <div class="white-text">
                      Vis programmer
                    </div>
                  </div>
                </div>
                </center>
              <% end %>
              <div class="btn-green-days box" style="cursor:pointer;background:#f3f2f2;width:60%;margin:4px;border:1px solid #c3c2c2">
                  <%= link_to "Redigér event", edit_event_path(event), class: 'grey-text' %>
              </div>
              <div class="btn-green-days box" style="cursor:pointer;background:#dc7063;width:60%;margin:4px">
                  <%= link_to 'Slett event', event_path(event),
                data: {:confirm => 'Er du sikker?'}, :method => :delete, class: 'white-text' %>
              </div>
            </div>


            <div class="col-xs-6">
              <% event.days.each_with_index do |day, i| %>
                <% if event.id == day.event_id %>
                  <div style="background: #f3f2f2;padding:5px;border-radius: 5px:margin:4px;margin-top:10px">
                    <strong>Day <%= (i+1) %></strong>
                    <span style="font-size: 12px"><%= day.date.strftime("%A, %d. %B") %></span><br />
                    <div class="btn-green-events box" style="cursor:pointer;background:#0052CC;width:60%">
                      <%= link_to 'Tilføj program', new_event_day_program_path(event, day), class: 'white-text' %>
                    </div>
                  </div>
                <% end %>
              <% end %>
              <div class="btn-green-days box" style="cursor:pointer;background:#f3f2f2;width:60%;margin:4px;margin-top:10px;border:1px solid #c3c2c2">
                  <%= link_to "Tilføj dag", new_event_day_path(event), class: 'grey-text' %>
              </div>
              <p id="notice"><%= notice %></p>
            </div>
          </div>
        </div>
      </div>
      <div class="mobileHideEvents">
        <center>
          <div class="row">
            <div class="col-xs-6">
              <h2><%= event.name %></h2><br />
              <div style="color:#c3c2c2;font-size: 12px"> ID: <%= event.id %></div>
              <strong>Adresse: <%= event.address %></strong><br />
              <strong>Billettkjøp:</strong> <%= event.ticket if event.ticket %><br />
              <strong><%= link_to "Kort oversigt", event if event.map %></strong><br />
              <%= event.programs.count %> program<%= 'mer' if event.programs.count > 1 %><br />

              <% if event.programs.any? %>
                <div onclick="toggle_div('programs-<%= event.id %>');">
                  <div class="btn-green-days box" style="cursor:pointer;background:#c3c2c2;width:60%;margin:4px;margin-top:10px">
                    <div class="white-text">
                      Vis programmer
                    </div>
                  </div>
                </div>
              <% end %>
              <div class="btn-green-days box" style="cursor:pointer;background:#f3f2f2;width:60%;margin:4px;margin-top:10px;border:1px solid #c3c2c2">
                  <%= link_to "Redigér event", edit_event_path(event), class: 'grey-text' %>
              </div>
              <div class="btn-green-days box" style="cursor:pointer;background:#dc7063;width:60%;margin:4px;margin-top:10px">
                  <%= link_to 'Slett event', event_path(event),
                data: {:confirm => 'Er du sikker?'}, :method => :delete, class: 'white-text' %>
              </div>
            </div>


            <div class="col-xs-6">
              <% event.days.each_with_index do |day, i| %>
                <% if event.id == day.event_id %>
                  <div style="background: #f3f2f2;padding:5px;border-radius: 5px:margin:4px;margin-top:10px">
                    <strong>Day <%= (i+1) %></strong>
                    <span style="font-size: 12px"><%= day.date.strftime("%A, %d. %B") %></span><br />
                    <div class="btn-green-events box" style="cursor:pointer;background:#0052CC;width:60%">
                      <%= link_to 'Tilføj program', new_event_day_program_path(event, day), class: 'white-text' %>
                    </div>
                  </div>
                <% end %>
              <% end %>
              <div class="btn-green-days box" style="cursor:pointer;background:#f3f2f2;width:60%;margin:4px;margin-top:10px;border:1px solid #c3c2c2">
                  <%= link_to "Tilføj dag", new_event_day_path(event), class: 'grey-text' %>
              </div>
              <p id="notice"><%= notice %></p>
            </div>
          </div>
          <%= image_tag event.image.url(:messenger), class: 'event-image-mobile' %>
        </center>
      </div>
      <div id="programs-<%= event.id %>" style="display:none">
        <center>
          <hr style="border: 1px solid #f3f2f2">
          <h3>Programmer</h3>

          <div class="row">
            <div class="col-xs-6 col-sm-4">
            </div>
          </div>
        </center>
        <% @programs.each do |program| %>
          <% if event.id == program.event_id %>
            <% if program.day == event.days.first %>
              <div class="col-md-3 col-sm-4" style="padding-top:20px">
                <div class="thumbnail">
                  <% if program.image? %>
                    <%= link_to (image_tag program.image.url(:messenger)), event_day_program_path(program.event_id, program.day_id, program), class: 'speaker-image' %>
                  <% else %>
                    <%= link_to (image_tag '/thumbnail.png'), event_day_program_path(program.event_id, program.day_id, program), class: 'speaker-image' %>
                  <% end %>
                  <div class="caption">
                    <div style="font-size: 16px"><%= link_to truncate(program.name, :length => 22), event_day_program_path(program.event_id, program.day_id, program), class: 'shorten' %></div>
                  </div>

                  <div style="font-size:14px;padding-left: 8px"> 
                    Sortering: <%= program.sorting %><br />
                    <div style="color:#999"> Dag 1 - <%= program.day.date %></div>
                  </div>
                  <%= link_to "Vis", event_day_program_path(program.event_id, program.day_id, program), class: 'btn-green-programs box' %>
                  <%= link_to "Redigér", edit_event_day_program_path(program.event_id, program.day_id, program), class: 'btn-green-programs box' %>
                  <%= link_to 'Slett', event_day_program_path(program.event_id, program.day_id, program),
                  data: {:confirm => 'Er du sikker? Dette kan ikke fortrydes.'}, :method => :delete, class: 'btn-red box' %>
                    <%#div class="col-xs-8 col-sm-6">
                      <% &#9889; rating %>
                    <%#/div>
                    <%#div class="col-xs-4 col-sm-6"%>
                    <%#/div%>
                </div>
              </div>
            <% end %>
          <% end %>
        <% end %>

        <% @programs.each do |program| %>
          <% if event.id == program.event_id %>
            <% if program.day == event.days.second %>
              <div class="col-md-3 col-sm-4" style="padding-top:20px">
                <div class="thumbnail">
                  <% if program.image? %>
                    <%= link_to (image_tag program.image.url(:messenger)), event_day_program_path(program.event_id, program.day_id, program), class: 'speaker-image' %>
                  <% else %>
                    <%= link_to (image_tag '/thumbnail.png'), event_day_program_path(program.event_id, program.day_id, program), class: 'speaker-image' %>
                  <% end %>
                  <div class="caption">
                    <div style="font-size: 16px"><%= link_to truncate(program.name, :length => 22), event_day_program_path(program.event_id, program.day_id, program), class: 'shorten' %></div>
                  </div>

                  <div style="font-size:14px;padding-left: 8px"> 
                    Sortering: <%= program.sorting %><br />
                    <div style="color:#999"> Dag 2 - <%= program.day.date %></div>
                  </div>
                  <%= link_to "Vis", event_day_program_path(program.event_id, program.day_id, program), class: 'btn-green-programs box' %>
                  <%= link_to "Redigér", edit_event_day_program_path(program.event_id, program.day_id, program), class: 'btn-green-programs box' %>
                  <%= link_to 'Slett', event_day_program_path(program.event_id, program.day_id, program),
                  data: {:confirm => 'Er du sikker? Dette kan ikke fortrydes.'}, :method => :delete, class: 'btn-red box' %>
                    <%#div class="col-xs-8 col-sm-6">
                      <% &#9889; rating %>
                    <%#/div>
                    <%#div class="col-xs-4 col-sm-6"%>
                    <%#/div%>
                </div>
              </div>
            <% end %>
          <% end %>
        <% end %>

        <% @programs.each do |program| %>
          <% if event.id == program.event_id %>
            <% if program.day == event.days.third %>
              <div class="col-md-3 col-sm-4" style="padding-top:20px">
                <div class="thumbnail">
                  <% if program.image? %>
                    <%= link_to (image_tag program.image.url(:messenger)), event_day_program_path(program.event_id, program.day_id, program), class: 'speaker-image' %>
                  <% else %>
                    <%= link_to (image_tag '/thumbnail.png'), event_day_program_path(program.event_id, program.day_id, program), class: 'speaker-image' %>
                  <% end %>
                  <div class="caption">
                    <div style="font-size: 16px"><%= link_to truncate(program.name, :length => 22), event_day_program_path(program.event_id, program.day_id, program), class: 'shorten' %></div>
                  </div>

                  <div style="font-size:14px;padding-left: 8px"> 
                    Sortering: <%= program.sorting %><br />
                    <div style="color:#999"> Dag 3 - <%= program.day.date %></div>
                  </div>
                  <%= link_to "Vis", event_day_program_path(program.event_id, program.day_id, program), class: 'btn-green-programs box' %>
                  <%= link_to "Redigér", edit_event_day_program_path(program.event_id, program.day_id, program), class: 'btn-green-programs box' %>
                  <%= link_to 'Slett', event_day_program_path(program.event_id, program.day_id, program),
                  data: {:confirm => 'Er du sikker? Dette kan ikke fortrydes.'}, :method => :delete, class: 'btn-red box' %>
                    <%#div class="col-xs-8 col-sm-6">
                      <% &#9889; rating %>
                    <%#/div>
                    <%#div class="col-xs-4 col-sm-6"%>
                    <%#/div%>
                </div>
              </div>
            <% end %>
          <% end %>
        <% end %>

        <% @programs.each do |program| %>
          <% if event.id == program.event_id %>
            <% if program.day == event.days.fourth %>
              <div class="col-md-3 col-sm-4" style="padding-top:20px">
                <div class="thumbnail">
                  <% if program.image? %>
                    <%= link_to (image_tag program.image.url(:messenger)), event_day_program_path(program.event_id, program.day_id, program), class: 'speaker-image' %>
                  <% else %>
                    <%= link_to (image_tag '/thumbnail.png'), event_day_program_path(program.event_id, program.day_id, program), class: 'speaker-image' %>
                  <% end %>
                  <div class="caption">
                    <div style="font-size: 16px"><%= link_to truncate(program.name, :length => 22), event_day_program_path(program.event_id, program.day_id, program), class: 'shorten' %></div>
                  </div>

                  <div style="font-size:14px;padding-left: 8px"> 
                    Sortering: <%= program.sorting %><br />
                    <div style="color:#999"> Dag 4 - <%= program.day.date %></div>
                  </div>
                  <%= link_to "Vis", event_day_program_path(program.event_id, program.day_id, program), class: 'btn-green-programs box' %>
                  <%= link_to "Redigér", edit_event_day_program_path(program.event_id, program.day_id, program), class: 'btn-green-programs box' %>
                  <%= link_to 'Slett', event_day_program_path(program.event_id, program.day_id, program),
                  data: {:confirm => 'Er du sikker? Dette kan ikke fortrydes.'}, :method => :delete, class: 'btn-red box' %>
                    <%#div class="col-xs-8 col-sm-6">
                      <% &#9889; rating %>
                    <%#/div>
                    <%#div class="col-xs-4 col-sm-6"%>
                    <%#/div%>
                </div>
              </div>
            <% end %>
          <% end %>
        <% end %>

        <% @programs.each do |program| %>
          <% if event.id == program.event_id %>
            <% if program.day == event.days.fifth %>
              <div class="col-md-3 col-sm-4" style="padding-top:20px">
                <div class="thumbnail">
                  <% if program.image? %>
                    <%= link_to (image_tag program.image.url(:messenger)), event_day_program_path(program.event_id, program.day_id, program), class: 'speaker-image' %>
                  <% else %>
                    <%= link_to (image_tag '/thumbnail.png'), event_day_program_path(program.event_id, program.day_id, program), class: 'speaker-image' %>
                  <% end %>
                  <div class="caption">
                    <div style="font-size: 16px"><%= link_to truncate(program.name, :length => 22), event_day_program_path(program.event_id, program.day_id, program), class: 'shorten' %></div>
                  </div>

                  <div style="font-size:14px;padding-left: 8px"> 
                    Sortering: <%= program.sorting %><br />
                    <div style="color:#999"> Dag 5 - <%= program.day.date %></div>
                  </div>
                  <%= link_to "Vis", event_day_program_path(program.event_id, program.day_id, program), class: 'btn-green-programs box' %>
                  <%= link_to "Redigér", edit_event_day_program_path(program.event_id, program.day_id, program), class: 'btn-green-programs box' %>
                  <%= link_to 'Slett', event_day_program_path(program.event_id, program.day_id, program),
                  data: {:confirm => 'Er du sikker? Dette kan ikke fortrydes.'}, :method => :delete, class: 'btn-red box' %>
                    <%#div class="col-xs-8 col-sm-6">
                      <% &#9889; rating %>
                    <%#/div>
                    <%#div class="col-xs-4 col-sm-6"%>
                    <%#/div%>
                </div>
              </div>
            <% end %>
          <% end %>
        <% end %>

      </div>
    </div>
    <div style="padding:30px"></div>
  <% end %>
<% end %>