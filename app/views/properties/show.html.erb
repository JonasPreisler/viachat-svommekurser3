<p id="notice"><%= notice %></p>
<div style="background: #f3f2f2;padding:15px;border-radius:10px;color:#1c1c1c;" class="property-form">

  <div class="row">
    <div class="col-md-6 col-sm-6 property">
      <p>
        <strong>Address:</strong><br />
        <%= @property.address %>
      </p>

      <p>
        <strong>Description:</strong><br />
        <%= @property.description %>
      </p>

      <p>
        <strong>Price:</strong><br />
        DKK <%= @property.price %>
      </p>

      <%= link_to 'Edit', edit_property_path(@property), class: 'btn-primary box' %> 
      <%= link_to 'Delete', property_path(@property),
      data: {:confirm => 'Are you sure?'}, :method => :delete, class: 'btn btn-danger' %>
    </div>
    <div class="col-md-6 col-sm-6">

      <p>
        <% @property_images.each do |p| %><br />
        <%= image_tag p.image_url(:thumb), class: 'image-border' %> 
        <%= link_to "Delete", property_image_path(p), :method => :delete %><br />
      <% end %>
      </p>

      <%= simple_form_for(@property, :html => { :multipart => true }) do |f| %>

        <br />

        <div class="links">
          <span style="font-size:14px;background:#bbb;padding:14px;border-radius:5px;"><%= link_to_add_association 'ADD IMAGE', f, :property_images %></span>
           <%= f.submit 'Upload', class: 'button' %>
        </div>
      <% end %>

    </div>
  </div>
</div>